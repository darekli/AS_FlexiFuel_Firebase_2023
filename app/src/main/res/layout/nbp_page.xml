<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">
    <Button
        android:id="@+id/button_home_page"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Home page" />

    <DatePicker
        android:id="@+id/datePicker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/button_home_page"
        android:layout_alignStart="@+id/button_home_page"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="48dp"
        android:layout_marginTop="26dp"
        android:layout_marginEnd="61dp"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/fetchCurrencyButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/currencySpinner"
        android:layout_marginTop="46dp"
        android:text="Fetch Average Currency" />

    <TextView
        android:id="@+id/exchange_rate_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/currencySpinner"
        android:layout_alignStart="@+id/currencySpinner"
        android:layout_marginStart="24dp"
        android:layout_marginTop="18dp"
        android:text="Exchange rate" />

    <TextView
        android:id="@+id/user_currency_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/fetchCurrencyButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:textSize="20sp" />

    <Spinner
        android:id="@+id/currencySpinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/datePicker"
        android:layout_alignStart="@+id/datePicker"
        android:layout_alignEnd="@+id/datePicker"
        android:layout_marginStart="21dp"
        android:layout_marginTop="22dp"
        android:layout_marginEnd="212dp"
        android:entries="@array/currencies" />
</RelativeLayout>

